# 调率站产品需求文档（PRD）

> 本文档从现有代码、运行行为与维护制度中**反向还原**调率站的产品定义，用于认识产品全貌并指导后续演进。
> 视角定位：**现状盘点 + 演进方向**。只描述"现在是什么、应该是什么、未来可以往哪走"，不替代技术文档。

## 目录

1. [产品概述](#1-产品概述)
2. [用户与场景](#2-用户与场景)
3. [核心流程](#3-核心流程)
4. [功能需求](#4-功能需求)
5. [数据与依赖](#5-数据与依赖)
6. [非功能需求](#6-非功能需求)
7. [边界与非目标](#7-边界与非目标)
8. [指标与验收](#8-指标与验收)
9. [演进路线图](#9-演进路线图)
10. [附录](#10-附录)

---

## 1 产品概述

> 反向还原：从 `doc/调率站产品与计算逻辑.md` 提炼产品定位，从实际功能反向描述现状。

### 1.1 背景

《燕云十六声》的毕业配装计算依赖社区作者维护的 Excel 计算器（Violetta@片雲 及各流派表作者）。这些工作簿功能强但使用门槛高、结果不可保存、词条组合难以复用。调率站把 Excel 的数值逻辑固化成浏览器可运行的计算引擎，并叠加了装备档案、方案管理和培养决策能力。

### 1.2 产品定位

调率站是一套**装备档案、配装分析和培养决策工具**。它主要回答四个问题：

1. 当前角色的完整配装形成了怎样的最终面板？
2. 这套面板在指定流派的标准输出方式下能产生多少伤害？
3. 当前输出距离该流派的理论毕业水平还有多远？
4. 更换、培养或转律哪些装备最值得？

### 1.3 价值主张

| 维度 | 价值 |
| --- | --- |
| 计算结果 | 与社区 Excel 计算器同口径（随上游更新），本地浏览器运行，隐私友好 |
| 数据留存 | 装备档案、配装方案、转律状态本地保存，可完整备份与云端同步 |
| 决策辅助 | 从"看毕业率"进阶到"怎么提升"，提供词条收益、培养、转律、最佳配装 |
| 低门槛录入 | 手动录入 + 截图 OCR + 批量 OCR 三种录入方式 |

### 1.4 核心逻辑链

```text
装备配置
  ↓
最终面板
  ↓
指定流派输出模拟
  ↓
当前 DPS
  ↓
与理论毕业 DPS 比较
  ↓
毕业率
```

这条链路也是词条收益、培养建议、单件装备比较和最佳配装的共同基础。

### 1.5 现状盘点（已具备的能力）

- **双计算引擎**：默认 Q7 原版引擎（跟随 `yysls.leoq7.com`，仅赛季抗性本地覆盖为 2.45）；可选 Assistant 引擎（`yysls-assistant.cn` Panel 口径 + 本站 Excel 模块）。引擎隔离，切换刷新页面。
- **装备管理**：多角色、八槽位穿戴、装备等级（110/105/100/96）、承音、紫装、可转律资格、可用流派限制、词条录入。
- **毕业率分析（六标签）**：手动填写表格（含按词条数量/直接填面板双模式）、最佳配装、词条优先级、培养建议、转律建议、单件装备对比。
- **方案管理**：每角色每流派每版本最多 50 套命名方案，含转律覆盖层（非破坏性）。
- **数据能力**：完整 JSON 备份/恢复、单角色导入导出、云端备份（Supabase）、Excel 填表下载。
- **OCR**：单件截图识别 + 批量 OCR 导入。

### 1.6 产品愿景

> 让每个玩家都能用最低的录入成本，把"我穿什么"准确换算成"我毕业了多少、还差什么、怎么提升"，并把社区 Excel 的数值权威固化成一个可用、可存、可对比的日常工具。

### 1.7 现状局限（反向盘点中暴露的问题）

- 首页首屏可能出现"毕业率 0.00%"占位后再刷新为真实值（异步 WASM 就绪前的降级显示）。
- 产品定位名为"自用"，但实际对外可访问，品牌与帮助文案存在定位模糊。
- 依赖上游数值更新节奏，新版本跟进时效受社区发布约束。

---

## 2 用户与场景

> 反向还原：从功能布局与"自用装备管理台"定位推断用户画像；从 `index.html` 交互入口还原使用场景。

### 2.1 用户画像

#### 核心用户：深度配装玩家

- 玩《燕云十六声》，追求流派输出上限；
- 愿意投入时间刷装备、承音、定音、转律；
- 会关注社区计算器（Excel 表、B站作者）来规划毕业配置；
- 需要"我的装备到底什么水平、还差什么"的量化答案。

#### 边缘用户

- **新手中度玩家**：刚接触毕业率概念，需要"计算中…"等加载反馈与结果解读引导。
- **多角色玩家**：多个角色、多套方案，需要数据隔离与快速切换。
- **多设备玩家**：电脑录装备、手机查结果，需要云端备份与冲突保护。

### 2.2 典型使用场景

| # | 场景 | 用户目标 | 主要入口 |
| --- | --- | --- | --- |
| S1 | 首次录入装备 | 把手头八件装备快速建档 | 录入装备（手填 / OCR / 批量 OCR） |
| S2 | 查看当前毕业率 | 知道"我现在毕业了多少" | 主页毕业横幅（流派/表格/武库/心法/弓诀/套装） |
| S3 | 手动规划理想面板 | 不看实际装备，模拟"词条数量→毕业率" | 毕业率分析 → 手动填写表格 |
| S4 | 搜索最佳配装 | 从仓库里找出毕业率最高的八件组合 | 毕业率分析 → 最佳配装 |
| S5 | 分析词条优先级 | 知道接下来刷哪条词条收益最大 | 毕业率分析 → 词条优先级 |
| S6 | 获取培养建议 | 逐件装备"改什么最值得" | 毕业率分析 → 培养建议 |
| S7 | 转律决策 | 某件 110 级装备是否值得转律、转哪条 | 毕业率分析 → 转律建议 |
| S8 | 单件装备对比 | 候选装备放进当前配装里比较强弱 | 毕业率分析 → 单件装备对比 |
| S9 | 多方案管理 | 同一角色保留多套配装思路 | 面板 → 方案（新增/改名/删除） |
| S10 | 数据备份迁移 | 换设备/清浏览器不丢数据 | 导出/导入 → 完整备份；云备份 |
| S11 | 下载填好的 Excel | 把当前配装回填到社区计算表存档 | 下载填好的表格 |

### 2.3 用户故事（As a…I want…）

- 作为玩家，我希望首页直接显示当前毕业率，以便快速了解现状 → 主页毕业横幅。
- 作为玩家，我希望手动模拟"我刷到多少条某种词条"能立刻看到毕业率，以便规划培养 → 手动填写（按词条数量）。
- 作为玩家，我希望在转律前知道哪个副词条最值得转，以便不浪费资源 → 转律建议。
- 作为玩家，我希望最佳配装能区分"已转律/待转律/不考虑转律"，以便贴近我的真实资源 → 最佳配装三模式。
- 作为多角色玩家，我希望每个角色的装备、方案、转律状态互不干扰 → 角色级数据隔离。
- 作为换机用户，我希望一个 JSON 文件能完整恢复所有角色与方案，以便无缝迁移 → 完整备份/恢复。
- 作为多设备用户，我希望云端备份不要静默覆盖另一台设备的更新，以便数据安全 → 云备份冲突保护。

### 2.4 用户心智模型

用户把产品理解为三件事的串联：

1. **我的装备档案**（录入 → 仓库 → 穿搭）；
2. **我的毕业度量**（面板 → DPS → 毕业率）；
3. **我的提升路径**（词条收益 → 培养 → 转律 → 最佳配装）。

因此产品的信息架构与文案都应围绕"档案 / 度量 / 提升"组织，避免把技术名词（WASM、引擎、flowName）暴露给用户。

### 2.5 关键假设（待验证）

- 用户愿意为"正确性"牺牲首屏速度（接受异步加载后的刷新）；
- 大多数用户只关注当前流派，跨流派功能属于深度场景；
- 用户信任数值来自社区作者，理解"结果随上游版本变化"。

---

## 3 核心流程

> 反向还原：从 `doc/调率站代码实现说明.md` 与运行时行为还原关键流程；强调"所有分析入口必须复用同一计算链"。

### 3.1 主计算数据流

所有常规毕业率入口（主页、手动面板、词条收益、培养建议、转律建议、单件比较、最佳配装）统一走：

```text
页面/分析功能
  ↓
统一 options（equippedItems, className, flowName, flowVersion, bow, xinfa, setName, armory,
             赛季/PVP, 贷款定音, classInputOverrides, 转律覆盖后的装备）
  ↓
YYSLSExcelRuntime.calculate()
  ├─ calculatePanel()   ← yysls_panel.wasm：配置 → 最终面板（36项）
  └─ collectBonuses()   ← 定音/穿透/武器增效/奇术增伤/临时修正
       ↓
calculateFromPanel()
  ↓
当前流派/表格版本的 Excel WASM（11个，40项输入 → 5项输出）
  ↓
totalDamage / DPS / graduationRatio / RDPS / RDPS 毕业率
```

**硬约束**：任何分析入口不得自行复制一套简化毕业率公式，否则会与主页和最佳配装产生口径分叉。

### 3.2 启动与首屏

```text
页面加载
  ↓
加载 generated-calc-strings/metadata/best40-stats
  ↓
加载 excel-runtime.js → 后台异步拉取 yysls_panel.wasm
  ↓
app.min.js init() → 恢复最近角色 → 加载该角色当前流派方案 → updateStats()
  │
  ├─ WASM 未就绪 → 降级显示 0.00% / "计算中…"
  ├─ Panel WASM 就绪 → 立即预加载当前 flowName 的 Excel WASM → 重算
  └─ Excel WASM 就绪 → 显示真实毕业率
```

### 3.3 毕业率分析流程（六标签统一入口）

```text
毕业率分析弹窗
  ├─ 手动填写表格：构造/分配面板 → 统一计算链 → 展示毕业率
  ├─ 最佳配装：仓库候选 → 过滤 → 预编译 → 批量计算 → Top20 → 去重 → 复核
  ├─ 词条优先级：当前配置 ± 目标词条 → 差值排序
  ├─ 培养建议：逐部位模拟词条/定音改善 → 边际收益排序
  ├─ 转律建议：替换指定副词条 → 遍历合法目标 → 完整配装毕业率对比
  └─ 单件装备对比：替换单槽位 → 其余环境不变 → 重算 → 排序
```

### 3.4 最佳配装搜索流程

```text
按部位构造候选装备（应用流派/武器/实体互斥/词条数量/面板阈值）
  ↓
根据模式生成候选（不考虑转律 / 优化已转律 / 同时规划待转律）
  ↓
预编译装备（rawEntries / bonusEntries / 穿戴状态 / 品质）
  ↓
批量 calculateBestBuildCompiled()，以毕业率维护候选堆
  ↓
生成 Top20
  ↓
最终复核、去重（八件原始装备 ID + 承音状态）、展示
```

关键不变量：

- 同一件物理装备不能同时占据两个槽位（`getOriginalEquipId` 归并转律副本）；
- 缓存摘要必须包含流派环境、弓诀、限制、转律模式；
- 转律通过非破坏性覆盖层（`transmutationSelections`）参与计算，不修改装备库原对象；
- 取消搜索需终止后续批次与结果写入。

### 3.5 转律建议流程

```text
选择左侧装备部位
  ↓
待转律装备：遍历所有副词条 → 找最值得指定的槽位与目标
已转律装备：只分析状态中指定副词条 → 不允许算法改换槽位
  ↓
保持其余七件与环境不变，按当前流派/弓/套装/心法/赛季/贷款定音实时计算
  ↓
输出最佳目标、最终毕业率、相对原词条差值
```

### 3.6 数据备份与恢复流程

```text
完整备份（导出）：
  全部角色 + 每角色装备(保留ID) + 全部流派方案 + 转律状态 + 手动毕业率数据
  → 单个明文 JSON（格式 yysls-tiaolv-full-backup, schemaVersion:2，不含可重建缓存）

完整恢复（导入）：
  校验格式/版本/结构/装备引用 → 汇总预览（角色数/装备数/方案数/同名角色）
  → 确认 → 按角色覆盖并保留未含本地角色 → 失败回滚

云备份（Supabase）：
  localStorage 为即时源 → 变更后 1 分钟单向上传（SHA-256 去重）
  → 上传前比较本地哈希/上次基线/当前云端哈希 → 发现他设备更新则暂停并询问
  → 恢复/上传前保护快照 → 恢复成功才写入新基线
```

### 3.7 引擎切换流程

```text
切换计算数据源（Q7 原版 / 测试新版）
  ↓
保存引擎选择（yysls_calculator_engine）
  ↓
刷新页面，仅加载所选引擎
  ↓
禁止热切换，禁止跨引擎回退/混用
```

---

## 4 功能需求

> 反向还原：按 `index.html` 页面结构与交互入口盘点六大功能模块；每条给出验收标准。
> 需求层级：`必须` = 现状已具备且必须长期保留；`增强` = 现状已有但可优化；`建议` = 反向盘点中提出的演进方向。

### 4.1 装备管理

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 角色管理 | 必须 | 可新建/重命名/删除角色；删除需二次确认；角色级数据完全隔离 |
| 装备录入 | 必须 | 八槽位；主词条 1 + 副词条 4 + 定音 1；数值精度 0.01；品质金/紫 |
| 装备等级 | 必须 | 110/105/100/96 级；缺省旧数据按 105 处理；卡片显示等级标签 |
| 承音 | 必须 | 可勾选承音；承音值参与"培养后"口径计算 |
| 紫装 | 必须 | 紫装基础攻击差值（武器/环/佩）参与计算 |
| 可转律资格 | 必须 | 仅 110 级且勾选可转律可进入转律；非 110 级隐藏并清除该资格 |
| 可用流派限制 | 必须 | 多选限制装备可用流派；武器位置按武器规则禁用不兼容流派 |
| 词条来源合法性 | 必须 | 词条必须符合部位词条池与数量上限（详见 5.5） |
| OCR 识别 | 必须 | 截图识别词条并回填表单；识别结果需用户确认 |
| 批量 OCR | 必须 | 粘贴外部工具结果，一次导入多件装备 |
| 自动命名 | 必须 | 录入/修改弹窗一键按主词条+副词条缩写自动命名；缩写体现词条，三率前置、增效置后，相同词条数量前缀（如 `2大外`）；手动输入优先不被覆盖。当前角色左侧提供「全部重命名」按钮，经二次确认后一键重命名当前角色全部装备。新增装备保存时若未手动命名、以及批量 OCR 保存的装备，均默认按词条自动命名 |
| 装备库排序/过滤 | 必须 | 等级降序 + 品质 + 承音 + 首词条种类排序；可用/全流派开关 |

### 4.2 面板配置

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 流派选择 | 必须 | 10 个 PVE 流派；切换触发重算 |
| 表格版本选择 | 必须 | 每流派可切换版本（如牵丝翊 1.2/2.0）；版本决定计算口径 |
| 武库选择 | 必须 | 按流派给出武库选项 |
| 心法选择 | 必须 | 四本心法；可更换并提示不支持的心法 |
| 弓诀选择 | 必须 | 精准/会心/会意弓 |
| 套装选择 | 必须 | 套装静态属性进入面板 |
| 赛季开关 | 必须 | "提前获得下半赛季属性"；开启后毕业率虚高提示 |
| PVP 开关 | 必须 | "首领视为玩家"；改变面板与词条池显示 |
| 贷款满定音 | 必须 | 勾选后按假设满定音参与计算，与真实定音区分口径 |
| 面板校验提示 | 必须 | "面板不对？"入口给出排查指引 |

**验收标准（面板）**：所有入口（主页/手动/收益/培养/转律/比较/最佳配装）在相同输入下得到相同的最终面板与毕业率。

### 4.3 毕业率分析（六标签）

#### 4.3.1 手动填写表格

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 双模式 | 必须 | 按词条数量 / 直接填写面板；独立结果节点避免闪动 |
| 按词条数量 | 必须 | 数量 → 8 件理论装备合法分配（首词条 8/副 32/普通 40 上限） |
| 满值/承音值 | 必须 | 两种取值口径；三率/五维/攻击/神力快捷专栏 |
| 词条修正项 | 必须 | 不占普通词条数量的面板修正项；支持正负/小数；随组合预设保存 |
| 词条组合预设 | 必须 | 每角色每流派每版本最多 50 套命名组合；保存/切换/更新/重命名/删除 |
| 弓箭同步 | 必须 | 神力专栏弓箭选择与当前方案同步并立即重算 |

#### 4.3.2 最佳配装

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 全库遍历 | 必须 | 遍历仓库合法组合，找毕业率最高穿搭 |
| 转律三模式 | 必须 | 不考虑转律 / 自动优化已转律 / 同时规划待转律；按角色记住模式 |
| 实体装备互斥 | 必须 | 同一物理装备（含转律/承音副本）不能占两槽 |
| Top20 | 必须 | 按八件原始装备 ID + 承音状态去重后保留前 20 套并支持切换 |
| 需承音限制 | 必须 | 可限制方案最多需承音件数 |
| 词条数量限制 | 必须 | 高级设置可按主/副词条条数设最少/最多/固定 |
| 低等级强制承音 | 必须 | 低于 110 级未承音装备只生成"需承音"升级版参与搜索 |
| 指定转律目标 | 必须 | 可锁定某装备及其中一条副词条参与遍历 |
| 方案应用 | 必须 | "使用该方案"只写转律覆盖层，不修改装备库原对象 |

#### 4.3.3 词条优先级

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 新增收益 | 必须 | 当前配置 + 目标词条后的毕业率提升，按收益排序 |
| 移除损失 | 必须 | 当前配置 - 目标词条后的毕业率损失，按损失排序 |
| 口径统一 | 必须 | 复用统一计算链，不得简化公式 |

#### 4.3.4 培养建议

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 逐件建议 | 必须 | 逐件检查主/副/定音，比较可行改善对毕业率提升 |
| 排序与筛选 | 必须 | 按投入后收益排序；标注承音/转律依赖、三率溢出、稀缺词条争夺 |
| 100% 基准展示 | 必须 | 展示所选表格内置的 100% 毕业面板作参照 |

#### 4.3.5 转律建议

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 待转律分析 | 必须 | 待转律装备遍历所有副词条，给出最值得的槽位与最佳目标 |
| 已转律分析 | 必须 | 已转律装备只分析指定槽位，不允许算法改换 |
| 单部位隔离 | 必须 | 只替换当前分析部位，保持其余七件与环境不变 |
| 结果展示 | 必须 | 每个副词条显示最佳目标、最终毕业率、相对原词条差值 |

#### 4.3.6 单件装备对比

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 槽位替换 | 必须 | 保持其他七件与环境不变，只替换目标部位 |
| 冻结定音 | 必须 | 默认冻结当前部位定音；可关 |
| 假设满承音 | 必须 | 可将对比装备数值拉满至 94% 再比较 |
| 排序 | 必须 | 按替换后毕业率排序 |

### 4.4 方案管理

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 多方案 | 必须 | 每角色每流派每版本多套方案，命名/新增/删除/改名 |
| 方案保存 | 必须 | 保存穿搭、弓、套装、心法、武库、版本、赛季、PVP、贷款定音、转律覆盖层 |
| 转律覆盖层 | 必须 | `transmutationSelections` 非破坏性覆盖层；失效资格/槽位/目标回退原词条并提示 |
| 方案提示 | 必须 | 有转律模拟时显示 `#scheme-transmutation-summary` 模拟操作清单 |
| 未保存提示 | 必须 | 有改动未保存时显示 `*` 标记 |

### 4.5 数据能力

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 完整备份 | 必须 | 单文件 JSON 含全部角色/装备/方案/转律/手动数据；schemaVersion:2；不含可重建缓存 |
| 完整恢复 | 必须 | 先校验后写入，失败回滚；同名角色确认覆盖、非同名保留 |
| 单角色导入导出 | 必须 | 角色级迁移 |
| 云备份 | 必须 | Supabase 登录（仅管理员预建账号）、延迟自动上传、SHA-256 去重、历史快照、多设备冲突保护 |
| 下载填好的表格 | 必须 | 当前输入回填到对应 Excel 模板并下载 |
| 转律状态持久化 | 必须 | `zhuanlv_status_<account>` 槽位记录；角色改名时迁移 |

### 4.6 计算引擎

| 功能 | 需求 | 验收标准 |
| --- | --- | --- |
| 双引擎 | 必须 | Q7 原版 / 测试新版可切换；选择持久化；切换刷新页面 |
| 引擎隔离 | 必须 | 禁止跨引擎混用数据；禁止热切换；禁止跨引擎回退 |
| 引擎就绪反馈 | 必须 | WASM 加载期间显示"计算中…"；就绪后自动重算 |
| 首屏预加载 | 必须 | Panel WASM 就绪并恢复角色后立即预加载当前流派 Excel WASM，不得等首次点击 |

### 4.7 非功能需求摘要

详见 [6 非功能需求](#6-非功能需求)：性能（懒加载、位级回归门槛）、正确性（与上游 parity）、安全（本地优先、恢复事务）、兼容（移动端/浏览器）。

### 4.8 已移除功能（禁止回归）

| 功能 | 状态 |
| --- | --- |
| 转律 CD 提醒（7 天计时） | 已完整移除，任何上游同步不得重新引入 |
| 通用 Excel 字节码解释器 | 已删除，改按流派/表格版本独立编译 WASM |
| 旧 39MB 解释器及浏览器回退 | 已删除 |

详细红线见 [7 边界与非目标](#7-边界与非目标)。

---

## 5 数据与依赖

> 反向还原：从 `doc/调率站代码实现说明.md`、`doc/tiaolv-local-customizations.md`、目录结构与 manifest 提炼。

### 5.1 数据模型

#### 核心实体

- **角色（Account）**：数据隔离单元。角色列表存 `game_account_list`。
- **装备（Equip）**：稳定 ID、部位、品质、等级、武器类型、主词条、副词条、定音、承音、可转律资格、可用流派限制。稳定 ID 不得因导入/恢复/方案切换而变化。
- **配装方案（Scheme）**：八槽位穿搭 + 弓诀 + 套装 + 心法 + 武库 + 版本 + 赛季/PVP/贷款定音 + `transmutationSelections` 覆盖层。
- **转律状态**：已转律槽位记录（`zhuanlv_status_<account>`）。
- **手动毕业率配置**：按角色/流派/版本保存，含词条数量模式与组合预设。

#### 本地存储（按角色隔离）

| Key 模式 | 内容 |
| --- | --- |
| `game_account_list` | 角色列表 |
| `game_equip_data_<account>` | 装备仓库 |
| `game_sim_data_<account>` | 配装、方案、计算选项 |
| `zhuanlv_status_<account>` | 转律状态 |
| `grad_manual_form_v2_<account>_<class>_<flow>` | 手动毕业率配置 |
| `last_selected_account` | 最近角色 |
| `yysls_calculator_engine` | 引擎选择（q7/assistant，首次默认 Q7） |

#### 云备份（Supabase）

| 表 | 用途 |
| --- | --- |
| `backup_latest` | 每用户最新完整备份 |
| `backup_snapshots` | 历史快照，最多 20 份；自动快照间隔 ≥30 分钟 |

云备份原则：本地优先、SHA-256 去重、上传前基线比较、多设备分叉暂停自动覆盖、恢复成功才写新基线。

### 5.2 上游依赖

| 上游 | 用途 | 同步方式 |
| --- | --- | --- |
| `yysls.leoq7.com` | Q7 原版引擎完整上游（Panel 值、公式、WASM、DPS/RDPS/毕业率、工作簿、最佳配装评分） | `skills/tiaolv-q7-engine-update/` |
| `yysls-assistant.cn` | Assistant 引擎 Panel 数值参考 | `skills/tiaolv-upstream-update/` + `skills/tiaolv-sync/` |
| 社区 Excel 工作簿 | 11 份表格版本（技能倍率、时间轴、基准 DPS、毕业率公式） | `skills/tiaolv-excel-update/` |

**数值隔离约束**：

- 两套引擎之间不得混用数据；
- Q7 模式唯一本地数值覆盖：赛季抗性 `2.45`（上游 `2.15`）；
- 任何上游同步不得覆盖本地定制层（见 5.4）。

### 5.3 计算资产清单

| 资产 | 说明 |
| --- | --- |
| `assets/wasm/yysls_panel.wasm` | 第一步：配置 → 最终面板（36 项输出） |
| `assets/wasm/excel/*.wasm` | 第二步：11 个表格版本独立编译，40 项输入 → 5 项输出 |
| `assets/wasm/q7/yysls_calc.wasm` | Q7 原版单体 WASM（Q7 引擎专用） |
| `assets/engines/q7/*` | Q7 引擎前端与 manifest（`localOverrides.seasonResistance = 2.45`） |
| `assets/js/generated-calc-metadata.js` | 流派字段、默认值、基准、最大值、承音值、100% 毕业面板 |
| `assets/js/generated-calc-strings.js` | 字符串 ID 表 |
| `assets/js/generated-best40-stats.js` | 培养建议预计算数据 |
| `excels/*.xlsx` | 11 份社区工作簿（公式源） |
| `calculator/yysls-calc-rust/` | Rust 与生成器源码、回归测试语料 |
| `study/` | 上游数值参考快照（new/old 轮换、assistant 种子） |

### 5.4 本地定制层（必须长期保留）

上游同步不得覆盖以下本地功能（详见 `doc/tiaolv-local-customizations.md`）：

- `assets/js/local-customizations.js`：装备等级控件、完整备份/恢复、词条数量模式、词条汇总渲染、转律资格/状态追踪、承音绿色显示。
- `assets/js/cloud-backup.js`：Supabase 登录、自动备份、冲突保护、历史快照。
- `assets/js/best-build-algorithms.js`：最佳配装算法注册中心。
- 主脚本内定制：Top20、需承音筛选、最佳配装算法选择、转律三模式、排序、可用/全流派开关、pvp 选项、低等级强制承音。

### 5.5 词条来源与合法性（业务约束）

| 约束 | 说明 |
| --- | --- |
| 总容量 | 8 主 + 32 副 = 40 普通词条；定音额外不占名额 |
| 首领增伤 | 仅胫甲、腕甲，整套最多 2 条 |
| 全武学增效 | 仅环、佩，整套最多 2 条 |
| 奇术增伤 | 单体/群体仅冠胄、胸甲，各最多 2 条 |
| 武器武学增效 | 绑定对应武器，同种通常最多 1 条 |
| 同件装备 | 不能有两个相同副词条；主词条可与一个副词条同名 |
| 紫装基础攻击 | 武器 -10/-23、环 -13、佩 -20（金紫差异） |

### 5.6 版本与一致性要求

- `generated-*.js` 不得手改，必须改生成源并重新生成，保证元数据与 WASM 同版本；
- 元数据、两个 WASM、Excel 模板版本不一致会导致结果漂移；
- 构建可重复：相同 Excel/生成器/Rust 版本/编译参数应产生相同 WASM 哈希；
- 前 184 项配置/词条为兼容前缀，内部追加 18 项装备结构信息，共 202 项；公开 API 与存档格式不变。

---

## 6 非功能需求

> 反向还原：从 `doc/调率站代码实现说明.md`、`doc/tiaolv-local-customizations.md` 提炼。

### 6.1 性能

| 需求 | 要求 | 说明 |
| --- | --- | --- |
| 首屏启动 | 只加载 Panel WASM（约 28KB） | Excel WASM（11 个，gzip 均 <1MB）按需/预加载当前流派 |
| 首屏毕业率 | WASM 就绪前显示"计算中…"，就绪后自动重算 | 不得等首次点击才加载当前流派模块 |
| Excel 模块加载 | 当前流派/表格版本优先；同版本只实例化一次并复用 | 切换流派时只加载新模块，不下载其余 27MB |
| 批量搜索 | 固定 5020 组语料整体 ≤20s、单流派 ≤4s | 旧解释器约 3.83 倍速度提升（迁移期实测） |
| 缓存 | 最佳配装缓存摘要必须包含流派环境、弓诀、限制、转律模式、引擎 ID | 条件变化后不得复用旧结果 |

### 6.2 正确性

| 需求 | 要求 |
| --- | --- |
| 毕业率口径 | 优先 WASM `graduationRatio × 100`；无结果才回退 `dps ÷ baselineDps × 100` |
| 一致性 | 所有分析入口复用统一计算链，禁止口径分叉 |
| 位级一致 | Q7：36 项 Panel 输出与 5 项伤害输出 Float64 位级比对；Assistant：同样验收 |
| 抗性覆盖 | Q7 模式运行时与配置缺省抗性必须均为 2.45，不得回退上游 2.15 |
| 精度 | 百分比按小数写入；增伤按 damage-state 防重复除以抗性 |
| 溢出处理 | 三率超上限后按 `溢出白值 = 超限黄值 × 2.45` 换算，不从 DOM 反推 |

### 6.3 安全

| 需求 | 要求 |
| --- | --- |
| 本地优先 | 本地装备库为权威数据源；计算/OCR/搜索失败不得破坏已保存装备 |
| 恢复事务 | 完整恢复"先校验、再暂存、后写入、失败回滚"，禁止半量覆盖 |
| 云备份鉴权 | 仅管理员预建账号登录；前端只保存 Publishable key；数据隔离依赖 Supabase RLS |
| 冲突保护 | 上传前比较本地基线/当前云端哈希；发现他设备更新暂停自动覆盖；无本地角色禁止覆盖云端 |
| 方案非破坏 | 转律只用覆盖层，任何分析/预览不得修改装备库原对象 |
| 导入容错 | 旧数据先规范化并报告跳过项，不能因单件异常放弃全部有效数据 |

### 6.4 兼容性

| 需求 | 要求 |
| --- | --- |
| 移动端 | `768px` 以下纵向布局；弹窗、词条、流派网格响应式 |
| 浏览器 | 桌面端主流浏览器；OCR 依赖 Tesseract（异步加载） |
| 旧数据 | 旧存档迁移（裂石钧旧名→裂石钧、旧方案结构→新方案、缺等级→105、旧转律模型→新资格模型） |
| 旧备份 | 兼容旧表字段差异、旧哈希差异；恢复后写回新版稳定哈希 |

### 6.5 可维护性

| 需求 | 要求 |
| --- | --- |
| 引擎隔离 | Q7 / Assistant 独立目录、独立 manifest、独立构建，禁止混用 |
| 构建可重复 | 相同输入产生相同 WASM 哈希；牵丝霖固定 `opt-level=2` 与单 codegen unit |
| 文档防漂移 | 产品规则、技术实现、转律机制、定制清单、PRD 各自独立维护 |
| 治理脚本 | 每次改动运行 `check_tiaolv_customizations.sh`；发布走 `publish_site.sh` |
| 版本号 | 前端文件改动更新 `index.html` 中 `?v=`（格式 `YYYYMMDDHHmm`） |

---

## 7 边界与非目标

> 反向还原：从 `doc/调率站代码实现说明.md`、`doc/tiaolv-local-customizations.md` 提炼的红线与明确不做的事。

### 7.1 明确不做的事

| 不做 | 原因/依据 |
| --- | --- |
| 不做通用 Excel 字节码解释器 | 已改为按流派/表格版本独立编译 WASM；不得恢复通用解释器，遇到不支持公式不得静默返回 0 或复用旧缓存 |
| 不做 pvp 毕业率计算 | `pvp` 只作为装备"可用流派"标签，不进入 Panel、毕业率、最佳配装、培养建议、转律搜索 |
| 不做跨引擎热切换 | 切换引擎必须刷新页面，禁止热切换、禁止跨引擎回退/混用 |
| 不做公开注册 | 云备份仅管理员预建账号登录，不提供公开注册、验证码、找回密码 |
| 不覆盖上游前端 | 本站前端自主维护；任何上游同步不得复制对方前端、不得覆盖本地定制层 |
| 不做转律 CD 提醒 | 已完整移除，后续同步不得重新引入（转律状态追踪/建议/计算不属于删除范围） |
| 不导出可重建缓存 | 最佳 40、搜索结果等可重建缓存不进权威备份 |
| 不做计算缓存持久化 | 方案/装备/转律状态进备份，计算缓存一律可重建 |

### 7.2 数值隔离红线

| 红线 | 说明 |
| --- | --- |
| 引擎隔离 | Q7 引擎与 Assistant 引擎数据/口径不得混用 |
| 唯一覆盖 | Q7 模式唯一允许偏离上游的数值是赛季抗性 `2.45`（上游 `2.15`）；装备、武学、心法、套装、武库、原版 WASM、伤害公式、时间轴、毕业基准不得随之修改 |
| 快照保护 | 任一抓取或验收失败时不得替换有效快照；`study/` 快照遵守 old→new 轮换 |

### 7.3 结构边界

```text
用户看到的功能（本地定制层 + 主业务层）
  ├─ 不得被上游同步覆盖
  └─ 禁止混用两套引擎数据

计算引擎（双 WASM）
  ├─ yysls_panel.wasm（第一步，面板）
  ├─ assets/wasm/excel/*.wasm（第二步，11 个表格版本）
  └─ assets/wasm/q7/yysls_calc.wasm（Q7 原版单体，仅 Q7 引擎）
```

### 7.4 已移除功能的永久性

- **转律 CD**：入口、运行时、存储、备份字段均不存在；保留一次性历史数据清理迁移。
- **旧 39MB 解释器 / 旧生成语义源码 / 浏览器回退路径**：均已删除；测试目录只保留固定输入输出语料作位级回归裁判。

### 7.5 非目标澄清

- 本产品是**个人工具演进**，不做商业化运营、不做多租户注册体系；
- 数值权威依赖社区，**不承诺**比上游更快或更准，只承诺同口径跟随；
- 不提供托管后端（除 Supabase 备份外），核心计算全部在浏览器本地完成。

---

## 8 指标与验收

> 反向还原：从 `doc/调率站代码实现说明.md`、`doc/tiaolv-local-customizations.md`、`AGENTS.md` 提炼验收口径。
> 此章是"产品如何证明自己是对的"——所有数字均来自当前已建立的回归体系。

### 8.1 数值正确性指标

| 指标 | 门槛 | 说明 |
| --- | --- | --- |
| Q7 上游 parity | 36 项 Panel + 5 项伤害输出 Float64 位级零差异 | 10 个流派固定矩阵 + ≥100 万组随机配置 |
| Assistant Panel parity | 同上 | Panel 数据独立参考实现 |
| 抗性覆盖断言 | 运行时与配置缺省抗性必须为 2.45 | 浏览器链路断言，防止退回 2.15 |
| 迁移验收语料 | 10 个可比较流派、5020 组输入、25100 项输出，位模式差异为 0 | 破竹鸢 2.4 不复现旧解释器过期结果 |
| Panel 独立参考 | 600 固定矩阵 + 100 万随机 = 36,021,600 个面板字段，差异 0 | Excel 重建不得改变 Panel 哈希 |

### 8.2 性能指标

| 指标 | 门槛 |
| --- | --- |
| 首屏加载 | 只加载 Panel WASM；当前流派 Excel WASM 预加载，不下载其余 27MB |
| Excel 模块大小 | 11 个模块 gzip 均 <1MB（最大鸣金影 661,578 字节） |
| 固定语料耗时 | 5020 组整体 ≤20s、单流派 ≤4s（旧解释器相对 3.83 倍） |

### 8.3 构建与发布指标

| 指标 | 门槛 |
| --- | --- |
| 构建可重复 | 相同 Excel/生成器/Rust/编译参数 → 相同 WASM 哈希 |
| 版本一致性 | `generated-*.js` 只由生成源产出，与 WASM 同版本 |
| 前端版本号 | 改动文件更新 `?v=`（`YYYYMMDDHHmm`） |
| 定制检查 | `check_tiaolv_customizations.sh` 通过（含登录、延迟备份、哈希去重、latest/快照、条件更新、多设备冲突、账号归属、恢复前保护） |
| Hugo 构建 | 本地构建通过后才可发布 |

### 8.4 功能验收标准（节选）

| 功能 | 验收标准 |
| --- | --- |
| 毕业率入口 | 主页、手动面板、词条收益、培养建议、转律建议、单件比较、最佳配装用同一计算链得到一致结果 |
| 引擎切换 | 切换保存并刷新，仅加载所选引擎；Q7 模式抗性为 2.45 |
| 最佳配装 | 实体互斥、转律模式、Top20 去重、缓存摘要含环境键、取消搜索终止 |
| 转律 | 已转律只分析指定槽位；覆盖层不改装备库原对象；方案提示正确 |
| 完整备份/恢复 | 校验→写入→失败回滚；同名确认、非同名保留；角色改名迁移转律状态 |
| 云备份 | 本地优先、冲突暂停、恢复成功才写新基线 |
| 首页加载 | WASM 就绪前显示"计算中…"，就绪后自动显示真实毕业率 |

### 8.5 发布门禁（过程指标）

1. 数值/引擎改动：先验证（parity/位级回归）再轮换快照；
2. 同步上游：保留本地定制层与 `localOverrides.seasonResistance=2.45`；
3. 代码/数据改动：运行定制检查 + Hugo 构建；
4. 发布：`publish_site.sh` 只提交任务相关路径，不夹带无关改动。

### 8.6 用户侧指标（建议新增观测）

现状尚未建立埋点。建议后续观测（见 [9 演进路线图](#9-演进路线图)）：

- 首页"计算中…"状态持续时间与"0.00%"出现频率；
- 各分析标签使用率（手动/最佳配装/转律占比）；
- 引擎选择分布（Q7 vs Assistant）；
- 备份/OCR 使用率与失败率。

---

## 9 演进路线图

> 现状盘点基础上提出的演进方向。优先级：P0=正确性/数据安全，P1=体验，P2=扩展。
> 每项标注状态与理由，供产品决策。

### 9.1 待改进清单

#### P0 正确性与数据安全（当前已有机制，需持续守护）

| 项 | 状态 | 方向 |
| --- | --- | --- |
| 数值 parity 门禁 | 已有 | 每次上游同步强制位级回归；扩展固定样本覆盖 |
| 双引擎隔离 | 已有 | 维持 manifest 登记与跨引擎禁止混用；警惕新字段误入对方引擎 |
| 恢复事务 | 已有 | 保持"校验→暂存→写入→回滚"，新增恢复前后 diff 摘要 |
| 抗性覆盖断言 | 已有 | 浏览器链路自动断言 2.45，防止回归 |

#### P1 体验改进（反向盘点发现）

| 项 | 状态 | 方向 |
| --- | --- | --- |
| 首页"毕业率 0.00%"占位 | 待改 | WASM 未就绪时应显示"计算中…"而非 0.00%；就绪后自动重算（主脚本 `updateBannerLoading` 已存在，需核对所有入口统一） |
| 首次进入的加载反馈 | 部分 | 加载当前流派 Excel WASM 期间给出明确进度/状态，避免误判为计算错误 |
| 结果解读引导 | 待加 | 毕业率>100%、接近 100%、差异解释给到用户，降低误读 |
| "自用"定位文案 | 待定 | 产品对外可访问，建议明确"个人维护工具"说明与免责声明 |

#### P2 扩展方向（候选，未承诺）

| 项 | 方向 | 理由 |
| --- | --- | --- |
| 最佳配装算法扩展 | 注册中心已留接口（`YYSLSBestBuildAlgorithms.register`） | 可接入更优搜索算法替代 legacy-exhaustive |
| 多设备协同冲突可视化 | 已有多设备保护 | 可扩展为"本机/云端差异对比"界面 |
| 数据导出增强 | 已有 Excel 填表 | 可增加毕业率历史曲线、方案对比导出 |
| 埋点与使用观测 | 无 | 支持决策（见 8.6） |
| 新表格版本接入 | 按 `skills/tiaolv-excel-update/` | 每次新版本走完整编译与位级验收 |

### 9.2 路线图建议

```text
近期（P0 守护 + P1 体验）
  1. 首页加载占位统一为"计算中…"，消除 0.00% 误读
  2. 首屏/切换加载状态与错误提示完善
  3. 结果解读文案（>100%、接近上限、口径差异）

中期（P1~P2）
  4. 观察指标（引擎分布、标签使用率、OCR/备份成功率）
  5. 最佳配装算法候选评估
  6. 多设备差异对比界面

长期（随上游）
  7. 跟随 Q7 / Assistant 上游版本迭代
  8. 新流派/新表格版本接入
```

### 9.3 风险与缓解

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| 上游数值变化未同步 | 毕业率失真、口碑受损 | 强制同步流程 + parity 门禁 + 版本时间戳展示 |
| 上游同步覆盖本地定制 | 功能丢失 | `check_tiaolv_customizations.sh` + 定制清单守卫 |
| 文档事实漂移 | 决策依据失效 | PRD/产品/技术/定制文档各自独立、引用不复制 |
| 首屏体验差 | 新用户流失 | P1 加载占位与状态反馈优先 |
| 单一维护者风险 | 知识集中 | skills/ 制度化沉淀流程，降低交接成本 |

### 9.4 本期不做（受 [7 边界与非目标](#7-边界与非目标) 约束）

- 不做公开注册/商业化；
- 不做 pvp 毕业率；
- 不做转律 CD；
- 不恢复通用 Excel 解释器。

---

## 10 附录

### 10.1 术语表

| 术语 | 含义 |
| --- | --- |
| 毕业率 | 当前配置 DPS ÷ 理论毕业 DPS × 100% |
| DPS | 一轮期望总伤害 ÷ 输出轴时间 |
| RDPS | 团队辅助口径的秒伤指标 |
| 最终面板 | 配置 + 装备 + 心法/弓诀/套装/武库/赛季 + 流派规则换算后的 36 项面板 |
| 承音 | 词条需培养才能达到的目标值；用于规划口径 |
| 满值 | 词条理论最高值；用于研究上限口径 |
| 真实值 | 装备当前实际值；用于评价现状口径 |
| 定音 | 额外属性，不占 40 条普通词条名额 |
| 贷款满定音 | 假设定音满值参与计算的规划口径 |
| 转律 | 只替换指定副词条，不改变装备实体；分为不可转律/待转律/已转律 |
| 需承音 | 系统模拟的"承音版"装备（候选搜索中低于最高等级且未承音） |
| 武库 | 影响面板的武学库配置 |
| 弓诀 | 精准/会心/会意三种弓 |
| 引擎 | Q7 原版（默认）/ 测试新版两套隔离计算口径 |
| 表格版本 | 每个流派对应的社区 Excel 版本（如破竹鸢 2.4、牵丝翊 2.0） |

### 10.2 上游源站与参考

| 源 | 角色 |
| --- | --- |
| `yysls.leoq7.com` | Q7 原版引擎完整上游 |
| `yysls-assistant.cn` | Assistant 引擎 Panel 数值参考 |
| Violetta@片雲 及各流派表作者 | 社区 Excel 工作簿作者 |

### 10.3 11 份工作簿清单（当前 Excel 模块）

| 工作簿 | 流派/版本 |
| --- | --- |
| 鸣金虹 110 阶竞速轴属性毕业率进阶计算器 1.3 | 鸣金虹 |
| 鸣金影 110 阶竞速轴属性毕业率进阶计算器 1.5 | 鸣金影 |
| 裂石威 110 阶竞速轴属性毕业率进阶计算器 2.2 | 裂石威 |
| 裂石钧 110 阶竞速轴属性毕业率进阶计算器 1.15 | 裂石钧 |
| 破竹尘 110 阶竞速轴属性毕业率进阶计算器 2.0 | 破竹尘 |
| 破竹风 110 阶竞速轴属性毕业率进阶计算器 1.1 | 破竹风 |
| 破竹鸢 110 阶竞速轴属性毕业率进阶计算器 2.4 | 破竹鸢 |
| 牵丝玉 110 阶竞速轴属性毕业率进阶计算器 1.4 | 牵丝玉 |
| 牵丝翊 110 阶竞速轴属性毕业率进阶计算器 1.2 | 牵丝翊 1.2 |
| 牵丝翊 110 阶竞速轴属性毕业率进阶计算器 2.0 | 牵丝翊 2.0（默认） |
| 牵丝霖 110 阶竞速轴属性毕业率进阶计算器 1.1 | 牵丝霖 |

### 10.4 引擎生产结构

| 引擎 | 结构 |
| --- | --- |
| Q7 原版 | 独立原版单体 `assets/wasm/q7/yysls_calc.wasm` + `assets/engines/q7/*`（manifest 登记 `seasonResistance=2.45`） |
| Assistant | 1 个 Panel WASM（`yysls_panel.wasm`）+ 11 个 Excel WASM（`assets/wasm/excel/*.wasm`） |

### 10.5 关键文档索引

| 文档 | 内容 |
| --- | --- |
| `doc/调率站产品与计算逻辑.md` | 业务规则与计算口径 |
| `doc/调率站代码实现说明.md` | 技术架构与实现 |
| `doc/调率站详细说明书.md` | 文档总入口（若存在） |
| `doc/转律机制说明.md` | 转律完整规则 |
| `doc/tiaolv-local-customizations.md` | 本地定制保护清单 |
| `AGENTS.md` | AI 工作约定与治理制度 |
| `prd/` | 本产品需求文档 |

### 10.6 治理流程索引

| 流程 | 入口 |
| --- | --- |
| 发布 | `skills/auto-site-publish/scripts/publish_site.sh` |
| 定制检查 | `skills/tiaolv-sync/scripts/check_tiaolv_customizations.sh` |
| 上游更新（assistant） | `skills/tiaolv-upstream-update/scripts/update_upstream.sh` |
| Panel 同步 | `skills/tiaolv-sync/` |
| Q7 引擎更新 | `skills/tiaolv-q7-engine-update/` |
| Excel 计算器更新 | `skills/tiaolv-excel-update/SKILL.md` |
